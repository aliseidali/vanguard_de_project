  Python codes
1. APIcalls.py
 a)get_artist_ids
   - stores the selected artist ids in an array of dictionary

 b)get_artist_api
   - Returns the list of selected artists in an array of dictionary

 c)get_album_api
   - Returns list of albums in an array of dictionary

 d)get_track_api
    - Returns list of tracks in array of dictionary

e)get_track_feature_api
   - Returns a list of track features in array of dictionary

2. Transformations.py
- Used to map and trasform the data.

3. db.py

- Functions used to connect database, create table, create views, insert and retrive data
4. main.py
 - The main programm that will run the application